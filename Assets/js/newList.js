const listContainer = document.getElementById("listContainer");
const newNameInput = document.getElementById("listName");
const createBtn = document.getElementsById("createList");

createBtn.addEventListener("click", () => {
  const listName = newNameInput.value.trim(); //list name
});
